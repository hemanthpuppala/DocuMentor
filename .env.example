# =============================================================================
# DocuMentor – Environment variables (copy to .env and fill in values)
# =============================================================================
# Never commit .env. Use this file as a template only.
# =============================================================================

# --- Required: at least one LLM provider ---
# Primary: Google Gemini (used if set)
GOOGLE_API_KEY=

# Fallback: Groq (used if GOOGLE_API_KEY is not set)
GROQ_API_KEY=

# --- Optional: Web search (Brave Search API) ---
# Enables internet search in the research pipeline. Leave empty to skip.
BRAVE_API_KEY=

# --- Optional: LangSmith tracing ---
# Uncomment to send LangChain/LangGraph traces to LangSmith
# LANGSMITH_TRACING=true
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=
# LANGSMITH_PROJECT=DocuMentor

# --- Optional: App limits ---
# MAX_FILE_SIZE=10485760
# MAX_SESSIONS=100
# REQUEST_TIMEOUT=60
